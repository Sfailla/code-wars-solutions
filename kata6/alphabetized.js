//  NOT THERE YET!!

const alphabetized = s => {
	// let regex = /[\W]+/gi;
	// const str = [ ...s ];
	// const filtered = str.filter(val => !regex.test(val));
	// filtered.sort((a, b) => {
	// 	a = a.toLowerCase();
	// 	b = b.toLowerCase();
	// 	if (a < b) return -1;
	// 	if (a > b) return 1;
	// 	return 0;
	// });
	// console.log(filtered.join(''));
	// return filtered;
};

alphabetized('grounded');
