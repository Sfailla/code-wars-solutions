function multiples(m, n) {
	console.log('m => ', m);
	console.log('n => ', n);

	const array = [];
	const total = n * m;
	total;

	for (let i = n; i <= total; i++) {
		i;
		array.push(i);
	}
	array;
	return array;
}

console.log(multiples(-1, 5), [ 5, 10, 15 ]);
