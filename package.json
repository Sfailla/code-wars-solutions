{
	"name": "code-wars",
	"version": "1.0.0",
	"description": "solving algorithms at code wars",
	"main": "index.js",
	"scripts": {
		"start": "live-server --browser=chrome",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Steven Failla",
	"license": "ISC",
	"dependencies": {
		"live-server": "^1.1.0"
	}
}
